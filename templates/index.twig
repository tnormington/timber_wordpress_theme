{% extends "base.twig" %}

{% block content %}
	<h1 class="page-title">{{wp_title}}</h1>
	{% for post in posts %}
		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
	{% endfor %}
{% endblock %}