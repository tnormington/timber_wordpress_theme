{% extends "base.twig" %}

{% block content %}
    <h1 class="page-title">{{post.title}}</h1>
    {{post.content}}
    {% for post in posts %}
        {% include 'tease-post.twig' %}
    {% endfor %}
    <p class="center">
        <a class="view-all-link front-page__view-all-link no-hover" href="/blog">All Blog Posts</a>
    </p>

{% endblock %}